# Use a base image with Python installed
FROM python:3.11-slim

# Set the working directory in the container
WORKDIR /app

# Install IPython
RUN pip install ipython

# Default command to run IPython REPL
CMD ["ipython"]

